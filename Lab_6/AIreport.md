# AI Usage Report

## 1. Информация об использовании AI

ФИО студента: Kabylbek Zhantore  
Группа: 101  
Лаба: Lab_6.1 и 6.2  
Дата: 21/02/2026  

Использованный AI: ☑ ChatGPT (OpenAI)  
Версия/Дата: ChatGPT-5 mini, 2026-02-21  

---

## 2. Вопросы и ответы AI

**Вопрос 1:**  
Как использовать `useParams` и `useLoaderData` для маршрута `/courses/:id`?  
Что получил: ☑ объяснение концепции, ☑ синтаксис, ☑ рекомендации  
Кратко: AI объяснил, что `useParams()` возвращает параметры маршрута, а `useLoaderData()` — данные, загруженные через loader, привёл пример использования.  

**Вопрос 2:**  
Как сделать сортировку списка курсов через query-параметр `?sort=asc|desc` с `useSearchParams`?  
Что получил: ☑ объяснение концепции, ☑ синтаксис, ☑ рекомендации  
Кратко: AI объяснил, как читать и изменять query-параметры через `useSearchParams()`, показал пример сортировки и обновления URL.  

---

## 3. Процесс написания кода

1. Сначала я спросил AI про работу хуков и query-параметров.  
2. AI дал объяснения и примеры синтаксиса.  
3. Я самостоятельно написал код:  
   - `data.ts` с курсами и функцией `getCourseById`  
   - `CourseDetail.tsx` с `useParams` и `useLoaderData`  
   - `Courses.tsx` с `useSearchParams` для сортировки  
4. Проверял маршруты и сортировку в браузере.  
5. Проблем не возникло, весь код написал вручную.  
6. Итог: динамические маршруты и сортировка работают, ошибки для неверного ID выводятся.  

---

## 4. Что я изучил

1. Использование `useParams` и `loader` для динамических маршрутов.  
2. Использование `useSearchParams` для query-параметров.  
3. Разницу между URL-параметрами (обязательные) и query-параметрами (опциональные).  

---

## 5. Процент помощи AI

☑ 0-25% (AI только объяснял концепции, код писал сам)  

Описание: AI использовался как консультант для понимания хуков и query-параметров, весь код написан вручную и проверен самостоятельно.