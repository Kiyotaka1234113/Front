# AI_REPORT — Lab 7

**Инструмент:** ChatGPT (GPT-5.2)
**Дата:** 2026-02-28

---

## 1. Использованные подсказки (prompts)

* "Объясни принцип Code Splitting с использованием lazy() и Suspense."
* "Сгенерируй пример lazy loading с маршрутизацией в React."
* "Создай fallback UI со спиннером для Suspense."
* "Сгенерируй компонент Error Boundary на основе class component."
* "Добавь возможность повторной загрузки (retry) при ошибке."
* "Оформи AI report в академическом стиле как в предыдущих лабораторных."

---

## 2. Как ИИ помог выполнить задание

ИИ использовался как вспомогательный инструмент для:

* понимания принципа Code Splitting
* реализации lazy loading компонентов
* настройки Suspense fallback UI
* создания структуры маршрутизации
* генерации Error Boundary
* реализации механизма повторной загрузки (retry)

Также ИИ помог:

* правильно организовать архитектуру проекта
* избежать загрузки всех страниц сразу
* реализовать обработку ошибок без краша приложения
* оформить лабораторную работу в требуемом формате

При этом:

* логика работы lazy компонентов была проверена вручную
* работа Suspense протестирована
* обработка ошибок проверена через симуляцию

---

## 3. Что было сделано самостоятельно

Самостоятельно были выполнены:

* понимание принципа ленивой загрузки
* настройка маршрутизации
* тестирование отображения fallback UI
* проверка работы Error Boundary
* организация структуры Lab_07/task1 и task2

ИИ использовался **как инструмент поддержки**,
а не как замена понимания материала.

---

## 4. Вывод

Использование ИИ ускорило:

* реализацию lazy loading
* создание fallback UI
* внедрение Error Boundary
* структурирование проекта

Однако ключевым остаётся понимание:

* зачем нужен Code Splitting
* как работает Suspense
* когда использовать Error Boundary

Поскольку именно эти механизмы
повышают устойчивость и производительность
реальных React-приложений.
